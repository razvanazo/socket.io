// ─── Constante globale backend ────────────────────────────────────────────────

export const PORT = 8081;
export const WORD_LENGTH = 4;
export const COUNTDOWN_FROM = 10;
export const RECONNECT_WINDOW_MS = 30_000;
export const SURVIVAL_BONUS = 10;
export const MAX_ATTACK_LOG = 50;   // câte intrări păstrăm în Redis
export const SNAPSHOT_ATTACK_LOG = 30; // câte trimitem în snapshot
